<IfModule mod_rewrite.c>
    RewriteEngine On

    # Перенаправление всех запросов в директорию public/
    # Используется при настройке виртуального хоста Apache на корень проекта
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
